import{_ as t,o as a,c as s,a2 as i}from"./chunks/framework.DBSoX6Zw.js";const u=JSON.parse('{"title":"Plik Webapp — Architecture & Gotchas","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/webapp.md","filePath":"architecture/webapp.md"}'),o={name:"architecture/webapp.md"};function d(n,e,l,r,c,p){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="plik-webapp-—-architecture-gotchas" tabindex="-1">Plik Webapp — Architecture &amp; Gotchas <a class="header-anchor" href="#plik-webapp-—-architecture-gotchas" aria-label="Permalink to &quot;Plik Webapp — Architecture &amp; Gotchas&quot;">​</a></h1><blockquote><p>Non-obvious details, design decisions, and pitfalls that agents should know before iterating on this codebase. For system-wide overview, see the root <a href="./system.html">ARCHITECTURE.md</a>.</p></blockquote><hr><h2 id="tech-stack" tabindex="-1">Tech Stack <a class="header-anchor" href="#tech-stack" aria-label="Permalink to &quot;Tech Stack&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Layer</th><th>Tech</th></tr></thead><tbody><tr><td>Framework</td><td>Vue 3 (Composition API, <code>&lt;script setup&gt;</code>)</td></tr><tr><td>Router</td><td>Vue Router 4, hash history (<code>#/</code>)</td></tr><tr><td>Styling</td><td>Tailwind CSS v4 (via <code>@import &quot;tailwindcss&quot;</code>) with custom <code>@utility</code> and <code>@theme</code> blocks</td></tr><tr><td>Code Editor</td><td>CodeMirror 6 (<code>@codemirror/language-data</code> for syntax, <code>@codemirror/theme-one-dark</code>)</td></tr><tr><td>Build</td><td>Vite</td></tr><tr><td>HTTP</td><td><code>fetch()</code> for JSON APIs, <code>XMLHttpRequest</code> for file uploads (progress tracking)</td></tr><tr><td>Backend</td><td>Go (Plik server, serves the SPA from <code>webapp/dist/</code> via <code>http.FileServer</code>)</td></tr></tbody></table><hr><h2 id="routing-url-format" tabindex="-1">Routing &amp; URL Format <a class="header-anchor" href="#routing-url-format" aria-label="Permalink to &quot;Routing &amp; URL Format&quot;">​</a></h2><p>All routes use hash-history (<code>#/</code>):</p><table tabindex="0"><thead><tr><th>Route</th><th>View</th><th>Purpose</th></tr></thead><tbody><tr><td><code>/#/</code></td><td><code>RootView</code></td><td>Upload (no query) or Download (<code>?id=...</code>)</td></tr><tr><td><code>/#/login</code></td><td><code>LoginView</code></td><td>Local + OAuth login</td></tr><tr><td><code>/#/home</code></td><td><code>HomeView</code></td><td>User dashboard (uploads, tokens, account)</td></tr><tr><td><code>/#/admin</code></td><td><code>AdminView</code></td><td>Admin panel (stats, users, all uploads)</td></tr><tr><td><code>/#/clients</code></td><td><code>ClientsView</code></td><td>CLI client downloads</td></tr><tr><td><code>/#/cli-auth</code></td><td><code>CLIAuthView</code></td><td>Approve CLI device auth login</td></tr><tr><td><code>/#/upload/:id</code></td><td>(redirect)</td><td>Legacy URL → <code>/?id=:id</code></td></tr></tbody></table><p>Admin link (upload-level): <code>/#/?id=&lt;uploadId&gt;&amp;uploadToken=&lt;token&gt;</code></p><p><code>RootView.vue</code> checks <code>route.query.id</code> — if present, renders <code>DownloadView</code>; otherwise <code>UploadView</code>.</p><blockquote><p><strong>Gotcha</strong>: The router uses <code>createWebHashHistory()</code>, so all URLs include <code>#/</code>. The <code>base</code> in <code>api.js</code> is computed from <code>window.location.origin + pathname</code> (without hash), so API calls go to the correct backend path.</p></blockquote><h3 id="auth-navigation-guard" tabindex="-1">Auth Navigation Guard <a class="header-anchor" href="#auth-navigation-guard" aria-label="Permalink to &quot;Auth Navigation Guard&quot;">​</a></h3><p>When <code>config.feature_authentication</code> is <code>&quot;forced&quot;</code>, a <code>router.beforeEach</code> guard redirects unauthenticated users to <code>/#/login</code>. Exceptions:</p><ul><li>The login page itself (<code>to.name === &#39;login&#39;</code>)</li><li>CLI client downloads (<code>to.name === &#39;clients&#39;</code>) — so users can get the CLI without logging in</li><li>Download pages (<code>to.name === &#39;root&#39; &amp;&amp; to.query.id</code>) — so shared links still work</li></ul><p>CLI auth approval (<code>to.name === &#39;cli-auth&#39;</code>) always requires authentication regardless of auth mode.</p><p><strong>Redirect preservation</strong>: When the guard redirects to login, it saves the intended destination to <code>sessionStorage</code> (<code>plik-auth-redirect</code> key) instead of a URL query parameter. This is necessary because OAuth flows do a full-page round-trip through an external provider (Google, OIDC, OVH), and the server callback redirects back to <code>/#/login</code> — any hash-fragment query params would be lost during this round-trip. Using sessionStorage solves this uniformly for all auth methods (local login and OAuth).</p><hr><h2 id="upload-token-admin-auth" tabindex="-1">Upload Token (Admin Auth) <a class="header-anchor" href="#upload-token-admin-auth" aria-label="Permalink to &quot;Upload Token (Admin Auth)&quot;">​</a></h2><h3 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How it works&quot;">​</a></h3><p>The Plik server generates an <code>uploadToken</code> when an upload is created. This token grants admin access (add/remove files, delete upload). It is returned as part of the upload JSON response.</p><h3 id="token-lifecycle" tabindex="-1">Token lifecycle <a class="header-anchor" href="#token-lifecycle" aria-label="Permalink to &quot;Token lifecycle&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>UploadView creates upload → server returns { id, uploadToken, ... }</span></span>
<span class="line"><span>  ↓</span></span>
<span class="line"><span>Token stored in memory (tokenStore.js) via setToken(id, token)</span></span>
<span class="line"><span>  ↓</span></span>
<span class="line"><span>Router navigates to /?id=&lt;id&gt;  (NO token in URL)</span></span>
<span class="line"><span>  ↓</span></span>
<span class="line"><span>DownloadView reads token from getToken(id) → sends as X-UploadToken header</span></span></code></pre></div><h3 id="admin-url-sharing" tabindex="-1">Admin URL sharing <a class="header-anchor" href="#admin-url-sharing" aria-label="Permalink to &quot;Admin URL sharing&quot;">​</a></h3><p>The <strong>Admin URL</strong> (shown in DownloadView sidebar) is the only way to share admin access:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>http://host/#/?id=&lt;id&gt;&amp;uploadToken=&lt;token&gt;</span></span></code></pre></div><p>When someone opens an admin URL:</p><ol><li><code>onMounted</code> in DownloadView reads <code>uploadToken</code> from <code>route.query</code></li><li>Stores it in memory via <code>setToken(id, token)</code></li><li><strong>Immediately strips it from the URL</strong> via <code>router.replace()</code> to prevent accidental sharing</li></ol><h3 id="key-rules" tabindex="-1">Key rules <a class="header-anchor" href="#key-rules" aria-label="Permalink to &quot;Key rules&quot;">​</a></h3><ul><li><strong>Never persist tokens</strong> to <code>localStorage</code>, <code>sessionStorage</code>, or cookies</li><li><strong>Never leave tokens in the URL</strong> after initial load — strip immediately</li><li>Tokens are sent as <code>X-UploadToken</code> header, never in the request body</li><li>Token is <strong>per-tab, per-session</strong> — refreshing the page loses admin access (by design)</li><li><code>getUpload()</code> and other API calls pass the token so the server returns <code>admin: true</code> in the response</li></ul><hr><h2 id="file-status-values" tabindex="-1">File Status Values <a class="header-anchor" href="#file-status-values" aria-label="Permalink to &quot;File Status Values&quot;">​</a></h2><p>Files in the Plik API have a <code>status</code> field with 5 possible values:</p><table tabindex="0"><thead><tr><th>Status</th><th>Meaning</th><th>Displayed?</th></tr></thead><tbody><tr><td><code>missing</code></td><td>File entry created, waiting to be uploaded</td><td>✅ Yes (uploading UI)</td></tr><tr><td><code>uploading</code></td><td>File is currently being uploaded</td><td>✅ Yes (progress bar)</td></tr><tr><td><code>uploaded</code></td><td>File has been uploaded and is ready for download</td><td>✅ Yes</td></tr><tr><td><code>removed</code></td><td>File has been removed by user, not yet cleaned up</td><td>❌ No</td></tr><tr><td><code>deleted</code></td><td>File has been deleted from the data backend</td><td>❌ No</td></tr></tbody></table><h3 id="activefiles-computed-property" tabindex="-1">activeFiles computed property <a class="header-anchor" href="#activefiles-computed-property" aria-label="Permalink to &quot;activeFiles computed property&quot;">​</a></h3><p>During active uploads (<code>isAddingFiles</code>), the top panel only shows files the user can interact with:</p><ul><li><strong>Non-streaming</strong>: only <code>uploaded</code> files (must be complete on server)</li><li><strong>Streaming</strong>: <code>uploading</code> + <code>uploaded</code> (download works via live stream)</li></ul><p>When not uploading (e.g. friend viewing the download page), all non-removed files are shown:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> activeFiles</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> computed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">upload.value?.files) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> []</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> upload.value.files.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">filter</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">f</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (f.status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;removed&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ||</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> f.status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;deleted&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> false</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (isAddingFiles.value) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">      if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (upload.value.stream) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> f.status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;uploading&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ||</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> f.status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;uploaded&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> f.status </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;uploaded&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><blockquote><p><strong>Key design</strong>: Files &quot;move&quot; from the bottom pending panel to the top active list as they become ready — non-streaming files appear when uploaded, streaming files appear when they start uploading (since their download link is immediately valid).</p></blockquote><blockquote><p><strong>Gotcha</strong>: After deleting a file via the API, the server returns <code>&quot;ok&quot;</code> (plain text, not JSON). The file&#39;s status changes to <code>removed</code> server-side but the API <strong>does not return the updated file object</strong>. You must call <code>fetchUpload()</code> again to refresh the list.</p></blockquote><blockquote><p><strong>Note</strong>: If <code>activeFiles.length === 0</code> after fetching, DownloadView shows a &quot;No files in this upload&quot; message. It does <strong>not</strong> redirect to home — this allows cancel-all and empty uploads to work cleanly.</p></blockquote><hr><h2 id="api-error-handling" tabindex="-1">API Error Handling <a class="header-anchor" href="#api-error-handling" aria-label="Permalink to &quot;API Error Handling&quot;">​</a></h2><h3 id="the-two-pass-text→json-pattern" tabindex="-1">The two-pass text→JSON pattern <a class="header-anchor" href="#the-two-pass-text→json-pattern" aria-label="Permalink to &quot;The two-pass text→JSON pattern&quot;">​</a></h3><p>The Plik server returns errors as <strong>either JSON or plain text</strong> depending on the endpoint. The <code>apiCall</code> function handles this with a two-pass approach:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 1. Read as text first (always works)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> resp.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 2. Try to parse as JSON</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> body</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> JSON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(text)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    message </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> body.message </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">||</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> body </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">||</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> message</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 3. Fall back to raw text (e.g., &quot;upload abc123 not found&quot;)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    message </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> text </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">||</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> message</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><blockquote><p><strong>Why</strong>: Calling <code>resp.json()</code> first consumes the body stream. If it fails (plain text response), <code>resp.text()</code> would then also fail with &quot;body stream already read&quot;. The text-first approach avoids this.</p></blockquote><h3 id="network-error-wrapping" tabindex="-1">Network error wrapping <a class="header-anchor" href="#network-error-wrapping" aria-label="Permalink to &quot;Network error wrapping&quot;">​</a></h3><p><code>apiCall</code> wraps <code>fetch()</code> in a try/catch to convert the browser&#39;s generic <code>TypeError: Failed to fetch</code> into a user-friendly <code>&quot;Network error — server may be unreachable&quot;</code>. Without this, network failures (offline, DNS, server down) surface as cryptic browser errors.</p><h3 id="xhr-upload-errors" tabindex="-1">XHR upload errors <a class="header-anchor" href="#xhr-upload-errors" aria-label="Permalink to &quot;XHR upload errors&quot;">​</a></h3><p><code>uploadFile</code> uses XHR (not fetch) for progress tracking. The server returns <strong>plain text</strong> errors, not JSON, so the XHR error handler uses the same two-pass pattern: try <code>JSON.parse</code>, fall back to <code>xhr.responseText</code>. The <code>error</code> event (network failure) produces <code>&quot;Upload connection lost — check your network&quot;</code> instead of the generic browser error.</p><h3 id="error-display-format" tabindex="-1">Error display format <a class="header-anchor" href="#error-display-format" aria-label="Permalink to &quot;Error display format&quot;">​</a></h3><p>Error messages include the HTTP status code when available: <code>&quot;message (HTTP 404)&quot;</code>. File upload errors in the banner include the filename: <code>&quot;photo.jpg: file too big&quot;</code>. This gives users enough context to understand what went wrong and report issues.</p><h3 id="success-responses" tabindex="-1">Success responses <a class="header-anchor" href="#success-responses" aria-label="Permalink to &quot;Success responses&quot;">​</a></h3><p>Some endpoints return <strong>plain text</strong> on success:</p><ul><li><code>DELETE /upload/:id</code> → <code>&quot;ok&quot;</code></li><li><code>DELETE /file/:uploadId/:fileId/:fileName</code> → <code>&quot;ok&quot;</code></li></ul><p>The <code>apiCall</code> function handles this too:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> text</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> resp.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">text</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">text) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">try</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> JSON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">parse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(text) } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> text }</span></span></code></pre></div><h3 id="error-display" tabindex="-1">Error display <a class="header-anchor" href="#error-display" aria-label="Permalink to &quot;Error display&quot;">​</a></h3><p>Errors from <code>fetchUpload</code> are displayed inline (not redirecting). This shows the actual server message like <code>&quot;upload feafea not found&quot;</code> instead of a generic error.</p><h3 id="separated-error-states-in-downloadview" tabindex="-1">Separated error states in DownloadView <a class="header-anchor" href="#separated-error-states-in-downloadview" aria-label="Permalink to &quot;Separated error states in DownloadView&quot;">​</a></h3><p>DownloadView uses <strong>two separate error refs</strong> to avoid upload errors from hiding the upload content:</p><table tabindex="0"><thead><tr><th>Ref</th><th>Purpose</th><th>Display</th></tr></thead><tbody><tr><td><code>error</code></td><td>Page-level failures (e.g., <code>fetchUpload</code> fails, upload not found)</td><td>Full-page error state via <code>v-else-if=&quot;error&quot;</code> — replaces entire content</td></tr><tr><td><code>uploadError</code></td><td>Non-file operational errors (reserved for future use)</td><td>Dismissible inline banner within the upload content area</td></tr></tbody></table><blockquote><p><strong>Why two refs</strong>: The template uses <code>v-if=&quot;loading&quot;</code> / <code>v-else-if=&quot;error&quot;</code> / <code>v-else-if=&quot;upload&quot;</code> branching. If file upload errors set <code>error</code>, the <code>v-else-if=&quot;error&quot;</code> branch takes over and hides the sidebar + file list. The <code>uploadError</code> ref keeps errors in the <code>v-else-if=&quot;upload&quot;</code> block so the user retains context.</p></blockquote><h3 id="per-file-error-handling-with-retry" tabindex="-1">Per-file error handling with retry <a class="header-anchor" href="#per-file-error-handling-with-retry" aria-label="Permalink to &quot;Per-file error handling with retry&quot;">​</a></h3><p>File upload errors are shown <strong>per-file in the pending panel</strong>, not in a top banner. Failed files:</p><ul><li>Stay in the pending panel with <code>status: &#39;error&#39;</code> and a red error message</li><li>Have a <strong>Retry</strong> button (per-file) and a <strong>Retry Failed</strong> button (bulk)</li><li>Have a dismiss (X) button to remove them from the list</li><li>Keep <code>isAddingFiles = true</code> so they don&#39;t appear as &quot;Waiting for upload&quot; in the top panel</li><li>When retried, transition back to <code>status: &#39;toUpload&#39;</code> and re-enter the upload pool</li></ul><h3 id="upload-pool-architecture" tabindex="-1">Upload pool architecture <a class="header-anchor" href="#upload-pool-architecture" aria-label="Permalink to &quot;Upload pool architecture&quot;">​</a></h3><p>All upload logic is DRY across three entry points:</p><table tabindex="0"><thead><tr><th>Function</th><th>Purpose</th></tr></thead><tbody><tr><td><code>uploadFileEntry(file)</code></td><td>Shared helper: XHR upload, progress, success/error handling</td></tr><tr><td><code>uploadPendingFiles()</code></td><td>Pool manager: concurrency-limited batch with re-check loop</td></tr><tr><td><code>retryFile(file)</code> / <code>retryAllFailed()</code></td><td>Reset file(s) to <code>toUpload</code>, delegate to pool</td></tr></tbody></table><p>Key design decisions:</p><ul><li><strong><code>isUploading</code></strong> (non-reactive) guards pool re-entry. Separate from <code>isAddingFiles</code> (reactive, UI display).</li><li><strong><code>activeBasicAuth</code></strong> stored at component level so retries preserve password-protected upload credentials.</li><li><strong>Re-check loop</strong>: after each batch completes, the pool re-scans for <code>toUpload</code> files. This lets retries queue into the existing pool without bypassing <code>MAX_CONCURRENT</code>.</li><li><strong><code>cancelAllUploads</code></strong> calls <code>fetchUpload()</code> after a 200ms delay so the server has time to update metadata.</li></ul><hr><h2 id="file-upload-mechanics" tabindex="-1">File Upload Mechanics <a class="header-anchor" href="#file-upload-mechanics" aria-label="Permalink to &quot;File Upload Mechanics&quot;">​</a></h2><h3 id="two-url-patterns-for-uploading-files" tabindex="-1">Two URL patterns for uploading files <a class="header-anchor" href="#two-url-patterns-for-uploading-files" aria-label="Permalink to &quot;Two URL patterns for uploading files&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Scenario</th><th>URL pattern</th></tr></thead><tbody><tr><td>Initial upload (has fileId from <code>createUpload</code>)</td><td><code>POST /file/:uploadId/:fileId/:fileName</code></td></tr><tr><td>Adding files to existing upload (no fileId)</td><td><code>POST /file/:uploadId</code></td></tr></tbody></table><p>The <code>api.js</code> <code>uploadFile</code> function picks the right pattern:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (fileEntry.id) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    url </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> \`\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">base</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}/\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mode</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}/\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">upload</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">id</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}/\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fileEntry</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">id</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}/\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fileEntry</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">fileName</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    url </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> \`\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">base</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}/\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">mode</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}/\${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">upload</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">id</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="stream-vs-file-mode" tabindex="-1">Stream vs File mode <a class="header-anchor" href="#stream-vs-file-mode" aria-label="Permalink to &quot;Stream vs File mode&quot;">​</a></h3><p>The URL prefix changes based on whether the upload uses streaming:</p><ul><li>Normal: <code>/file/...</code></li><li>Streaming: <code>/stream/...</code></li></ul><h3 id="upload-flow-uploadview-→-downloadview" tabindex="-1">Upload flow (UploadView → DownloadView) <a class="header-anchor" href="#upload-flow-uploadview-→-downloadview" aria-label="Permalink to &quot;Upload flow (UploadView → DownloadView)&quot;">​</a></h3><ol><li><code>buildUploadParams()</code> pre-populates files (with <code>reference</code> fields) so the server assigns IDs upfront</li><li><code>createUpload(params)</code> → server returns upload with <code>id</code>, <code>uploadToken</code>, and pre-created file entries (with IDs)</li><li><code>setPendingFiles(id, files, basicAuth)</code> stashes files in the in-memory <code>pendingUploadStore</code> — file IDs are matched via <code>reference</code> (not array index)</li><li><code>setToken(id, token)</code>, then <code>router.push({ path: &#39;/&#39;, query: { id } })</code> — <strong>navigates immediately</strong></li><li>DownloadView mounts, calls <code>consumePendingFiles(id)</code> to retrieve the stashed files</li><li>Auto-starts <code>uploadPendingFiles()</code> — uploads files concurrently (max 5 at a time) with a worker pool</li><li>Status updates are <strong>local</strong> (reactive mutations on <code>upload.value.files[i].status</code>) — no <code>fetchUpload()</code> during uploads to avoid UI flash</li><li>For streaming uploads: <code>onStart</code> callback marks server files as <code>&#39;uploading&#39;</code> → they appear in the top panel immediately</li><li>For all uploads: <code>.then()</code> marks server files as <code>&#39;uploaded&#39;</code> → they appear in the top panel</li><li>One final <code>fetchUpload()</code> after all uploads complete to sync with server truth</li></ol><blockquote><p><strong>Key design</strong>: UploadView does NO file uploading — it only stages files and creates the upload. All upload logic lives in DownloadView, reusing the same <code>uploadPendingFiles()</code> used when adding files to an existing upload.</p></blockquote><h3 id="pending-upload-store-pendinguploadstore-js" tabindex="-1">Pending Upload Store (<code>pendingUploadStore.js</code>) <a class="header-anchor" href="#pending-upload-store-pendinguploadstore-js" aria-label="Permalink to &quot;Pending Upload Store (\`pendingUploadStore.js\`)&quot;">​</a></h3><p>In-memory store (same pattern as <code>tokenStore.js</code>) to pass files from UploadView → DownloadView across navigation:</p><ul><li><code>setPendingFiles(uploadId, files, basicAuth, passphrase)</code> — stash after <code>createUpload()</code> (includes E2EE passphrase if enabled)</li><li><code>consumePendingFiles(uploadId)</code> — retrieve and clear (one-shot)</li></ul><h3 id="staged-upload-flow-downloadview" tabindex="-1">Staged upload flow (DownloadView) <a class="header-anchor" href="#staged-upload-flow-downloadview" aria-label="Permalink to &quot;Staged upload flow (DownloadView)&quot;">​</a></h3><p>When adding files to an existing upload:</p><ol><li><code>onFilesSelected</code> stages files in <code>pendingFiles</code> ref (NOT uploaded yet)</li><li>User sees staged files with remove buttons, can review before uploading</li><li>Clicking &quot;Upload&quot; runs <code>uploadPendingFiles()</code> which uploads files concurrently (max 5) with local status updates</li><li>Files transition from bottom panel → top panel as they become ready</li><li>Files added to existing uploads have <strong>no pre-created fileId</strong> — server assigns one</li></ol><h3 id="upload-cancellation" tabindex="-1">Upload Cancellation <a class="header-anchor" href="#upload-cancellation" aria-label="Permalink to &quot;Upload Cancellation&quot;">​</a></h3><p>The <code>uploadFile()</code> function in <code>api.js</code> returns <code>{ promise, abort }</code>:</p><ul><li><code>promise</code> — resolves to file metadata on success</li><li><code>abort()</code> — calls <code>xhr.abort()</code>, rejecting with <code>{ cancelled: true }</code></li></ul><p>Cancel buttons in <code>FileRow.vue</code> emit a <code>cancel</code> event for individual files. A &quot;Cancel All&quot; button in the pending files header aborts all in-progress uploads.</p><blockquote><p><strong>Gotcha</strong>: When a file upload is aborted via <code>xhr.abort()</code>, the server needs time to detect the broken connection and clean up the file status (<code>uploading</code> → <code>removed</code> → <code>deleted</code>). The <code>cancelFileUpload()</code> function waits 200ms before calling <code>fetchUpload()</code> to avoid showing stale <code>uploading</code> status in <code>activeFiles</code>.</p></blockquote><hr><h2 id="staged-file-object-shape" tabindex="-1">Staged File Object Shape <a class="header-anchor" href="#staged-file-object-shape" aria-label="Permalink to &quot;Staged File Object Shape&quot;">​</a></h2><p>Files stored locally before upload use this shape (NOT the server shape):</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  reference</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ref-1707123456-1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Local unique ID (from generateRef())</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  fileName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;photo.jpg&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  size</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1048576</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  file</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: File,                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// The browser File object</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  status</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;toUpload&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,             </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// &#39;toUpload&#39; | &#39;uploading&#39; | &#39;uploaded&#39; | &#39;error&#39;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  progress</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,                    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 0-100 upload progress</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  abort</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,                    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Set during upload — calls xhr.abort()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><blockquote><p><strong>Gotcha</strong>: Local files use <code>reference</code> as a key, not <code>id</code>. The <code>id</code> is only assigned by the server after upload. The <code>size</code> field is <code>size</code> locally but <code>fileSize</code> in server responses.</p></blockquote><hr><h2 id="filename-length-limit" tabindex="-1">Filename Length Limit <a class="header-anchor" href="#filename-length-limit" aria-label="Permalink to &quot;Filename Length Limit&quot;">​</a></h2><p>Filenames are capped at <strong>1024 characters</strong> — enforced client-side at multiple points:</p><table tabindex="0"><thead><tr><th>Location</th><th>Enforcement</th></tr></thead><tbody><tr><td><code>UploadView.addFiles()</code></td><td>Truncates <code>file.name</code> to 1024 chars when files are added to the staging list</td></tr><tr><td><code>FileRow.onNameInput()</code></td><td>Truncates on blur (when editing finishes)</td></tr><tr><td><code>FileRow.onNameKeydown()</code></td><td>Blocks character input at limit (allows Backspace/Delete/ctrl keys)</td></tr><tr><td><code>FileRow.onNamePaste()</code></td><td>Intercepts paste, calculates available space, clamps inserted text</td></tr></tbody></table><blockquote><p><strong>Note</strong>: The server also validates filename length and returns a 400 if exceeded. The client-side enforcement prevents this from happening under normal use.</p></blockquote><hr><h2 id="feature-flags-config" tabindex="-1">Feature Flags (Config) <a class="header-anchor" href="#feature-flags-config" aria-label="Permalink to &quot;Feature Flags (Config)&quot;">​</a></h2><p>The server exposes feature flags via <code>GET /config</code>:</p><table tabindex="0"><thead><tr><th>Value</th><th>Meaning</th></tr></thead><tbody><tr><td><code>enabled</code></td><td>Feature is available, default off</td></tr><tr><td><code>disabled</code></td><td>Feature is hidden entirely</td></tr><tr><td><code>forced</code></td><td>Feature is on, user cannot toggle it off</td></tr><tr><td><code>default</code></td><td>Feature is available, default on</td></tr></tbody></table><p>| <code>feature_e2ee</code> | <code>&quot;enabled&quot;</code> or <code>&quot;disabled&quot;</code> — controls E2EE toggle in upload sidebar |</p><p>The config object keys use the pattern <code>feature_&lt;name&gt;</code> (e.g., <code>feature_one_shot</code>, <code>feature_stream</code>).</p><p>Helper functions (in <code>config.js</code>):</p><ul><li><code>isFeatureEnabled(name)</code> → returns <code>true</code> unless value is <code>&quot;disabled&quot;</code></li><li><code>isFeatureForced(name)</code> → returns <code>true</code> only if value is <code>&quot;forced&quot;</code></li><li><code>isFeatureDefaultOn(name)</code> → returns <code>true</code> if value is <code>&quot;default&quot;</code> or <code>&quot;forced&quot;</code> (controls initial toggle state)</li></ul><h3 id="other-config-keys" tabindex="-1">Other Config Keys <a class="header-anchor" href="#other-config-keys" aria-label="Permalink to &quot;Other Config Keys&quot;">​</a></h3><p>The <code>GET /config</code> response also includes:</p><p>| Key | Purpose | |-----|---------|| | <code>maxFileSize</code> | Max file size in bytes (shown in upload drop zone) | | <code>maxUserSize</code> | Max total size per user | | <code>maxTTL</code> | Max TTL in seconds | | <code>googleAuthentication</code> | <code>true</code> if Google OAuth is configured → shows Google login button | | <code>ovhAuthentication</code> | <code>true</code> if OVH OAuth is configured → shows OVH login button | | <code>feature_local_login</code> | <code>&quot;enabled&quot;</code> or <code>&quot;disabled&quot;</code> — controls local login form visibility (replaces old <code>localAuthentication</code> boolean) | | <code>oidcAuthentication</code> | <code>true</code> if OIDC is configured → shows OIDC login button | | <code>oidcProviderName</code> | Display name for OIDC button (e.g. <code>&quot;Keycloak&quot;</code>, defaults to <code>&quot;OpenID&quot;</code>) | | <code>downloadDomain</code> | Alternate domain for download URLs (set in <code>api.js</code> via <code>setDownloadDomain</code>) | | <code>abuseContact</code> | Abuse contact email → displayed in global footer (<code>App.vue</code>) |</p><hr><h2 id="size-ttl-limit-precedence" tabindex="-1">Size &amp; TTL Limit Precedence <a class="header-anchor" href="#size-ttl-limit-precedence" aria-label="Permalink to &quot;Size &amp; TTL Limit Precedence&quot;">​</a></h2><p>The server enforces layered limits: <strong>user-specific → server config</strong>. The special values <code>0</code> (use default) and <code>-1</code> (unlimited) are key.</p><h3 id="value-semantics" tabindex="-1">Value Semantics <a class="header-anchor" href="#value-semantics" aria-label="Permalink to &quot;Value Semantics&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Value</th><th>Meaning</th></tr></thead><tbody><tr><td><code>&gt; 0</code></td><td>Explicit limit (bytes for size, seconds for TTL)</td></tr><tr><td><code>0</code></td><td>Use server default</td></tr><tr><td><code>-1</code></td><td>Unlimited (no limit enforced)</td></tr></tbody></table><h3 id="precedence-rules-from-server-context-upload-go" tabindex="-1">Precedence Rules (from <code>server/context/upload.go</code>) <a class="header-anchor" href="#precedence-rules-from-server-context-upload-go" aria-label="Permalink to &quot;Precedence Rules (from \`server/context/upload.go\`)&quot;">​</a></h3><p><strong>MaxFileSize</strong> (<code>GetMaxFileSize()</code>):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>if user != nil &amp;&amp; user.MaxFileSize != 0 → user.MaxFileSize</span></span>
<span class="line"><span>else → config.MaxFileSize</span></span></code></pre></div><p><strong>MaxUserSize</strong> (<code>GetUserMaxSize()</code>):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>if user == nil → unlimited (-1)                    // anonymous = no user quota</span></span>
<span class="line"><span>if user.MaxUserSize &gt; 0 → user.MaxUserSize          // explicit user limit</span></span>
<span class="line"><span>if user.MaxUserSize &lt; 0 → unlimited (-1)            // user explicitly unlimited</span></span>
<span class="line"><span>if user.MaxUserSize == 0 → config.MaxUserSize        // fall back to server default</span></span></code></pre></div><p><strong>MaxTTL</strong> (inside <code>setTTL()</code>):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>maxTTL = config.MaxTTL</span></span>
<span class="line"><span>if user != nil &amp;&amp; user.MaxTTL != 0 → maxTTL = user.MaxTTL</span></span>
<span class="line"><span>if maxTTL &gt; 0 → enforce (reject infinite or over-limit TTL)</span></span>
<span class="line"><span>if maxTTL &lt;= 0 → no limit enforced</span></span></code></pre></div><h3 id="effective-limit-calculation-client-side" tabindex="-1">Effective Limit Calculation (Client-Side) <a class="header-anchor" href="#effective-limit-calculation-client-side" aria-label="Permalink to &quot;Effective Limit Calculation (Client-Side)&quot;">​</a></h3><p><code>UploadView.vue</code> computes effective limits via <code>auth.user</code> with fallback to <code>config</code>:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> effectiveMaxFileSize</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> computed</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> user</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> auth.user</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (user </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> user.maxFileSize </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> &amp;&amp;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> user.maxFileSize </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> user.maxFileSize</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> config.maxFileSize</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">})</span></span></code></pre></div><p>The same pattern applies for <code>effectiveMaxTTL</code>, which is passed as a prop to <code>UploadSidebar</code>.</p><h3 id="size-unit-convention-si-1000-based" tabindex="-1">Size Unit Convention (SI / 1000-based) <a class="header-anchor" href="#size-unit-convention-si-1000-based" aria-label="Permalink to &quot;Size Unit Convention (SI / 1000-based)&quot;">​</a></h3><div class="important custom-block github-alert"><p class="custom-block-title">IMPORTANT</p><p>All size formatting uses <strong>SI units</strong> (1 GB = 1,000,000,000 bytes), matching the server&#39;s <code>go-humanize</code> library (<code>humanize.ParseBytes</code> / <code>humanize.Bytes</code>). The <code>GB</code> constant in edit modals is <code>1000³</code>, not <code>1024³</code>.</p></div><p>This means:</p><ul><li>Config <code>MaxFileSizeStr = &quot;10GB&quot;</code> → 10,000,000,000 bytes → displays &quot;10.00 GB&quot; everywhere</li><li>Admin enters &quot;1&quot; GB in edit modal → stores 1,000,000,000 bytes → shows &quot;1.00 GB&quot;</li></ul><div class="caution custom-block github-alert"><p class="custom-block-title">CAUTION</p><p>Never use 1024-based division with &quot;GB&quot; labels. If you need binary units, use &quot;GiB&quot; labels with 1024-based math.</p></div><hr><h2 id="component-architecture" tabindex="-1">Component Architecture <a class="header-anchor" href="#component-architecture" aria-label="Permalink to &quot;Component Architecture&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>App.vue</span></span>
<span class="line"><span>├── AppHeader.vue          — top nav bar (Upload, CLI, Source, user/admin links)</span></span>
<span class="line"><span>├── RootView.vue           — switches between Upload/Download based on query.id</span></span>
<span class="line"><span>│   ├── UploadView.vue     — file staging, settings, upload execution</span></span>
<span class="line"><span>│   │   ├── UploadSidebar  — upload settings (one-shot, stream, TTL, E2EE, etc.)</span></span>
<span class="line"><span>│   │   ├── FileRow        — individual file display</span></span>
<span class="line"><span>│   │   └── CodeEditor     — text paste mode with syntax highlighting</span></span>
<span class="line"><span>│   └── DownloadView.vue   — file list, admin actions</span></span>
<span class="line"><span>│       ├── DownloadSidebar — upload info (E2EE badge), share (passphrase + toggle), admin URL, actions</span></span>
<span class="line"><span>│       ├── FileRow         — file link (preview), caret (details), download/QR/copy/view/remove</span></span>
<span class="line"><span>│       ├── CodeEditor      — inline file viewer (read-only)</span></span>
<span class="line"><span>│       ├── QrCodeDialog    — QR code modal</span></span>
<span class="line"><span>│       ├── CopyButton      — clipboard copy with feedback</span></span>
<span class="line"><span>│       └── ConfirmDialog   — confirmation modal</span></span>
<span class="line"><span>├── LoginView.vue          — local login form + OAuth/OIDC buttons</span></span>
<span class="line"><span>├── HomeView.vue           — user dashboard (uploads/tokens/account)</span></span>
<span class="line"><span>│   └── CopyButton         — clipboard copy for tokens</span></span>
<span class="line"><span>├── AdminView.vue          — admin panel (stats/users/uploads)</span></span>
<span class="line"><span>├── ClientsView.vue        — CLI client downloads (from embedded build info)</span></span>
<span class="line"><span>└── CLIAuthView.vue        — CLI device auth approval (displays code, approves session)</span></span></code></pre></div><hr><h2 id="authenticated-pages" tabindex="-1">Authenticated Pages <a class="header-anchor" href="#authenticated-pages" aria-label="Permalink to &quot;Authenticated Pages&quot;">​</a></h2><h3 id="auth-state-authstore-js" tabindex="-1">Auth State (<code>authStore.js</code>) <a class="header-anchor" href="#auth-state-authstore-js" aria-label="Permalink to &quot;Auth State (\`authStore.js\`)&quot;">​</a></h3><p>Reactive singleton holding <code>auth.user</code> (set on login, cleared on logout). Checked by <code>main.js</code> on app load via <code>GET /me</code>. The header shows user/admin links when <code>auth.user</code> is set.</p><h3 id="loginview-login" tabindex="-1">LoginView (<code>/#/login</code>) <a class="header-anchor" href="#loginview-login" aria-label="Permalink to &quot;LoginView (\`/#/login\`)&quot;">​</a></h3><ul><li>Local login form (username + password → <code>POST /auth/local/login</code>) — <strong>hidden</strong> when <code>isFeatureEnabled(&#39;local_login&#39;)</code> returns <code>false</code> (i.e. <code>FeatureLocalLogin = &quot;disabled&quot;</code> on the server)</li><li>Conditional OAuth buttons (Google, OVH) based on <code>config.googleAuthentication</code> / <code>config.ovhAuthentication</code></li><li>OIDC button (label from <code>config.oidcProviderName</code>) → calls <code>GET /auth/oidc/login</code> to get the authorization URL, then <code>window.location.href</code> redirects to the OIDC provider</li><li>&quot;or continue with&quot; divider only shown when both local login and at least one OAuth/OIDC provider are enabled</li><li>Redirects to the stored <code>sessionStorage</code> destination on success via <code>consumeRedirect()</code>, or <code>/</code> if none</li></ul><h3 id="homeview-home" tabindex="-1">HomeView (<code>/#/home</code>) <a class="header-anchor" href="#homeview-home" aria-label="Permalink to &quot;HomeView (\`/#/home\`)&quot;">​</a></h3><p>Sidebar + main content layout (same pattern as download view).</p><p><strong>Sidebar</strong>: user avatar, login/provider, name, email, admin badge, stats (uploads/files/size). Buttons: Upload files, Uploads, Tokens, Sign out, Edit account, Delete uploads, Delete account.</p><p><strong>Uploads tab</strong>: paginated user uploads via <code>GET /me/uploads</code>. Supports token-based filtering. Each upload shows files, date, size, with clickable token labels.</p><p><strong>Tokens tab</strong>: list/create/revoke tokens via <code>GET|POST|DELETE /me/token</code>. Token comment displayed above UUID. Click token to filter uploads by it.</p><p><strong>Edit Account modal</strong>: name, email, password (local only). Admin users additionally see maxFileSize, maxUserSize, maxTTL, admin toggle. Saves via <code>POST /user/{id}</code>.</p><blockquote><p><strong>Gotcha</strong>: Non-admin users cannot change quota fields or admin status — the server enforces this; the UI hides those fields.</p></blockquote><h3 id="adminview-admin" tabindex="-1">AdminView (<code>/#/admin</code>) <a class="header-anchor" href="#adminview-admin" aria-label="Permalink to &quot;AdminView (\`/#/admin\`)&quot;">​</a></h3><p>Admin-only page. Redirects non-admins to <code>/</code> on mount.</p><p><strong>Sidebar</strong>: server version/build info (release + mint badges), nav buttons (Stats, Uploads, Users), Create User button.</p><p><strong>Stats tab</strong>: server config (maxFileSize, maxUserSize, defaultTTL, maxTTL) + server statistics (users, uploads, files, totalSize, anonymous counts).</p><p><strong>Users tab</strong>: paginated user list via <code>GET /users</code>. Each row shows login, provider, name, email, quotas, admin badge. Actions: Impersonate (👤), Edit (opens modal with full quota controls), Delete (with confirmation). Delete disabled for self. Impersonate disabled for self.</p><p><strong>Uploads tab</strong>: paginated all-uploads via <code>GET /uploads</code>. Sort by date/size, order asc/desc. Filter by user/token (clickable links in each row). Each row shows upload ID (link), dates, user, token, files with sizes, Remove button.</p><p><strong>Create User modal</strong>: provider (select), login, password (local only), name, email, quotas (maxFileSize, maxUserSize, maxTTL), admin toggle. Creates via <code>POST /user</code>.</p><p><strong>Edit User modal</strong>: same as HomeView edit but with full admin quota controls always visible.</p><h3 id="impersonation" tabindex="-1">Impersonation <a class="header-anchor" href="#impersonation" aria-label="Permalink to &quot;Impersonation&quot;">​</a></h3><p>Allows an admin to &quot;become&quot; another user to browse their uploads, test their quotas, or manage their account. The feature spans four files:</p><p><strong>Flow:</strong></p><ol><li>Admin clicks 👤 on a user row in AdminView</li><li><code>authStore.impersonate(user)</code> stores the target user and calls <code>api.setImpersonateUser(userId)</code></li><li><code>api.js</code> injects <code>X-Plik-Impersonate: &lt;userId&gt;</code> header on <strong>every</strong> subsequent API request</li><li>Server middleware (<code>server/middleware/impersonate.go</code>) detects the header, verifies the caller is an admin, and switches the request context to the impersonated user</li><li><code>GET /me</code> now returns the impersonated user — <code>authStore.user</code> updates accordingly</li><li>A yellow banner in <code>AppHeader.vue</code> shows &quot;⚠️ Impersonating <strong>username</strong>&quot; with a <strong>Stop</strong> button</li></ol><p><strong>State management (<code>authStore.js</code>):</strong></p><ul><li><code>auth.originalUser</code> — preserved real admin identity (never changes during impersonation)</li><li><code>auth.impersonatedUser</code> — the user object being impersonated (null when not impersonating)</li><li><code>auth.user</code> — switches to the impersonated user during impersonation</li><li><code>clearImpersonate()</code> — resets header, restores <code>auth.user</code> to <code>auth.originalUser</code></li></ul><p><strong>API layer (<code>api.js</code>):</strong></p><ul><li><code>setImpersonateUser(userId)</code> — sets/clears a module-level <code>_impersonateUserId</code></li><li><code>apiCall()</code> — if <code>_impersonateUserId</code> is set, adds <code>X-Plik-Impersonate</code> header</li></ul><h3 id="api-endpoints-auth-admin" tabindex="-1">API Endpoints (Auth/Admin) <a class="header-anchor" href="#api-endpoints-auth-admin" aria-label="Permalink to &quot;API Endpoints (Auth/Admin)&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Endpoint</th><th>Method</th><th>Purpose</th><th>Auth</th></tr></thead><tbody><tr><td><code>/auth/local/login</code></td><td>POST</td><td>Local login</td><td>—</td></tr><tr><td><code>/auth/oidc/login</code></td><td>GET</td><td>Get OIDC authorization URL</td><td>—</td></tr><tr><td><code>/auth/oidc/callback</code></td><td>GET</td><td>OIDC callback (sets session)</td><td>—</td></tr><tr><td><code>/auth/google/login</code></td><td>GET</td><td>Get Google authorization URL</td><td>—</td></tr><tr><td><code>/auth/ovh/login</code></td><td>GET</td><td>Get OVH authorization URL</td><td>—</td></tr><tr><td><code>/auth/logout</code></td><td>GET</td><td>Logout</td><td>Session</td></tr><tr><td><code>/me</code></td><td>GET</td><td>Get current user</td><td>Session</td></tr><tr><td><code>/me</code></td><td>DELETE</td><td>Delete account</td><td>Session</td></tr><tr><td><code>/me/uploads</code></td><td>GET</td><td>User uploads (paginated)</td><td>Session</td></tr><tr><td><code>/me/uploads</code></td><td>DELETE</td><td>Delete all user uploads</td><td>Session</td></tr><tr><td><code>/me/token</code></td><td>GET</td><td>List tokens</td><td>Session</td></tr><tr><td><code>/me/token</code></td><td>POST</td><td>Create token</td><td>Session</td></tr><tr><td><code>/me/token/{token}</code></td><td>DELETE</td><td>Revoke token</td><td>Session</td></tr><tr><td><code>/user/{id}</code></td><td>POST</td><td>Update user</td><td>Session</td></tr><tr><td><code>/stats</code></td><td>GET</td><td>Server statistics</td><td>Admin only</td></tr><tr><td><code>/users</code></td><td>GET</td><td>List all users (paginated)</td><td>Admin only</td></tr><tr><td><code>/user</code></td><td>POST</td><td>Create user</td><td>Admin only</td></tr><tr><td><code>/user/{id}</code></td><td>DELETE</td><td>Delete user</td><td>Admin only</td></tr><tr><td><code>/uploads</code></td><td>GET</td><td>All uploads (paginated, filterable)</td><td>Admin only</td></tr></tbody></table><blockquote><p><strong>Gotcha</strong>: XSRF token (from <code>plik-xsrf</code> cookie) must be sent as <code>X-XSRFToken</code> header on all mutating requests (POST, DELETE). This is handled automatically in <code>apiCall()</code>.</p></blockquote><hr><h2 id="responsive-layout" tabindex="-1">Responsive Layout <a class="header-anchor" href="#responsive-layout" aria-label="Permalink to &quot;Responsive Layout&quot;">​</a></h2><p>The layout uses a <strong>mobile-first stacking pattern</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Mobile (&lt;768px):     [Sidebar]     (full width, stacked on top)</span></span>
<span class="line"><span>                     [Main Content] (full width, below)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Desktop (≥768px):   [Sidebar | Main Content]  (side by side)</span></span></code></pre></div><p>Key classes:</p><ul><li>Containers: <code>flex flex-col md:flex-row</code></li><li>Sidebars: <code>w-full md:w-72 md:shrink-0</code></li><li>Outer wrapper: <code>overflow-x-hidden</code> (prevents long URLs from causing horizontal scroll)</li><li>FileRow: inner container uses <code>flex-wrap</code>, &quot;Download&quot; text hidden on mobile (<code>hidden md:inline</code>)</li></ul><hr><h2 id="css-custom-utilities" tabindex="-1">CSS Custom Utilities <a class="header-anchor" href="#css-custom-utilities" aria-label="Permalink to &quot;CSS Custom Utilities&quot;">​</a></h2><p>The <code>style.css</code> file defines custom utility classes via <code>@utility</code> (Tailwind v4 syntax):</p><table tabindex="0"><thead><tr><th>Utility</th><th>Description</th></tr></thead><tbody><tr><td><code>glass-card</code></td><td>Semi-transparent card with backdrop blur</td></tr><tr><td><code>btn</code></td><td>Base button styles</td></tr><tr><td><code>btn-primary</code></td><td>Accent-colored button (cyan)</td></tr><tr><td><code>btn-success</code></td><td>Green button</td></tr><tr><td><code>btn-danger</code></td><td>Red button</td></tr><tr><td><code>btn-ghost</code></td><td>Transparent hover button</td></tr><tr><td><code>toggle-switch</code></td><td>Toggle switch base</td></tr><tr><td><code>toggle-dot</code></td><td>Toggle switch dot (animated)</td></tr><tr><td><code>input-field</code></td><td>Styled text input</td></tr><tr><td><code>sidebar-section</code></td><td>Glass-card styled sidebar section</td></tr><tr><td><code>file-row</code></td><td>Glass-card styled file row with hover effect</td></tr></tbody></table><blockquote><p><strong>Gotcha</strong>: These are <code>@utility</code> blocks, NOT traditional CSS classes or Tailwind <code>@apply</code>. They follow Tailwind v4&#39;s custom utility syntax and generate single utility classes.</p></blockquote><hr><h2 id="code-editor-file-viewer" tabindex="-1">Code Editor &amp; File Viewer <a class="header-anchor" href="#code-editor-file-viewer" aria-label="Permalink to &quot;Code Editor &amp; File Viewer&quot;">​</a></h2><h3 id="codeeditor-component" tabindex="-1">CodeEditor Component <a class="header-anchor" href="#codeeditor-component" aria-label="Permalink to &quot;CodeEditor Component&quot;">​</a></h3><p>Reusable CodeMirror 6 wrapper (<code>CodeEditor.vue</code>) used in two contexts:</p><table tabindex="0"><thead><tr><th>Context</th><th>View</th><th>Mode</th><th>Purpose</th></tr></thead><tbody><tr><td>Text paste</td><td>UploadView</td><td>Read-write</td><td>Paste/edit text before uploading as a file</td></tr><tr><td>File viewer</td><td>DownloadView</td><td>Read-only</td><td>Preview uploaded text files inline</td></tr></tbody></table><p><strong>Props</strong>: <code>modelValue</code> (v-model), <code>filename</code> (drives syntax highlighting), <code>readonly</code>, <code>placeholder</code></p><p><strong>Language switching</strong>: Uses a <code>Compartment</code> to reconfigure the language extension dynamically when <code>filename</code> changes — no editor destruction/recreation needed, preserving cursor position and undo history.</p><p><strong>Content-based language detection</strong>: Uses <code>highlight.js</code> (lazy-loaded via dynamic <code>import()</code> on first detection call) for accurate auto-detection of ~190 languages. Detection fires via a 1s debounce on content changes. In UploadView, auto-detection only updates the filename when it still matches the default <code>paste.*</code> pattern.</p><p><strong>JSON prettify / validate</strong>: When the detected language is JSON, two action buttons appear in the editor header bar. <strong>Validate</strong> (<code>JSON.parse()</code> only) checks syntax and shows a brief green &quot;Valid&quot; flash on success or a dismissable red error banner on failure — it never changes the content. <strong>Prettify</strong> (<code>JSON.parse()</code> → <code>JSON.stringify(…, null, 2)</code>) validates <em>and</em> reformats the content with 2-space indentation. In read-only mode (DownloadView file viewer) prettify updates the displayed view only — it does not modify the file on the server.</p><p><strong>Auto-display</strong>: In <code>DownloadView.vue</code>, if an upload contains exactly one text file, the viewer panel opens automatically on mount (or when the file finishes uploading). A watcher on <code>activeFiles</code> triggers <code>viewFile()</code> for the first file if it&#39;s the only one and it&#39;s a text file.</p><h3 id="text-file-detection" tabindex="-1">Text-File Detection <a class="header-anchor" href="#text-file-detection" aria-label="Permalink to &quot;Text-File Detection&quot;">​</a></h3><p>The <code>isTextFile()</code> utility in <code>utils.js</code> determines if a file can be viewed in the code editor based on:</p><ol><li><strong>Size</strong>: Max 5 MB (<code>MAX_VIEWABLE_SIZE</code>)</li><li><strong>MIME type</strong>: <code>text/*</code> prefix only — the server detects MIME types via Go&#39;s <code>http.DetectContentType</code>, which returns <code>text/plain</code> for all text-like content (JS, JSON, Go, Python, etc.) and <code>application/octet-stream</code> for binary</li></ol><p><code>FileRow.vue</code> uses this to conditionally show a &quot;View&quot; button on uploaded files in download mode.</p><hr><h2 id="build-release-process" tabindex="-1">Build &amp; Release Process <a class="header-anchor" href="#build-release-process" aria-label="Permalink to &quot;Build &amp; Release Process&quot;">​</a></h2><h3 id="development" tabindex="-1">Development <a class="header-anchor" href="#development" aria-label="Permalink to &quot;Development&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> webapp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # Vite dev server on :5173, proxies API to :8080</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> server</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">go</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./plikd.cfg</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> # Go backend on :8080</span></span></code></pre></div><p>Vite proxy is configured in <code>vite.config.js</code> — all <code>/api</code>, <code>/auth</code>, <code>/file</code>, <code>/stream</code>, <code>/config</code>, <code>/me</code>, etc. calls are forwarded to the Go backend.</p><h3 id="production-build" tabindex="-1">Production Build <a class="header-anchor" href="#production-build" aria-label="Permalink to &quot;Production Build&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> frontend</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # cd webapp &amp;&amp; npm ci &amp;&amp; npm run build → webapp/dist/</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">make</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> server</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     # cd server &amp;&amp; go build → server/plikd</span></span></code></pre></div><p>The Go server serves <code>webapp/dist/</code> via <code>http.FileServer</code>. Default config: <code>WebappDirectory = &quot;../webapp/dist&quot;</code>.</p><h3 id="makefile-targets" tabindex="-1">Makefile Targets <a class="header-anchor" href="#makefile-targets" aria-label="Permalink to &quot;Makefile Targets&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Target</th><th>Purpose</th></tr></thead><tbody><tr><td><code>all</code></td><td><code>clean clean-frontend frontend clients server</code></td></tr><tr><td><code>frontend</code></td><td><code>npm ci &amp;&amp; npm run build</code> in <code>webapp/</code></td></tr><tr><td><code>server</code></td><td>Build Go binary <code>server/plikd</code></td></tr><tr><td><code>client</code></td><td>Build Go CLI client <code>client/plik</code></td></tr><tr><td><code>clients</code></td><td>Cross-compile clients for all architectures</td></tr><tr><td><code>docker</code></td><td>Build Docker image <code>rootgg/plik:dev</code></td></tr><tr><td><code>release</code></td><td>Create release archives via <code>releaser/release.sh</code></td></tr><tr><td><code>clean</code></td><td>Remove server/client binaries</td></tr><tr><td><code>clean-frontend</code></td><td>Remove <code>webapp/dist/</code></td></tr><tr><td><code>clean-all</code></td><td>Clean everything including <code>node_modules</code></td></tr></tbody></table><h3 id="build-info-client-downloads" tabindex="-1">Build Info &amp; Client Downloads <a class="header-anchor" href="#build-info-client-downloads" aria-label="Permalink to &quot;Build Info &amp; Client Downloads&quot;">​</a></h3><p>The server binary embeds a JSON blob (via <code>server/gen_build_info.sh</code>) containing a client list discovered from the <code>clients/</code> directory. The <code>ClientsView</code> page displays download links from this embedded build info.</p><p>For full details on the Docker multi-stage build and release packaging, see <a href="./releaser.html">releaser/ARCHITECTURE.md</a>.</p><hr><h2 id="common-pitfalls" tabindex="-1">Common Pitfalls <a class="header-anchor" href="#common-pitfalls" aria-label="Permalink to &quot;Common Pitfalls&quot;">​</a></h2><ol><li><p><strong>Don&#39;t call <code>resp.json()</code> then <code>resp.text()</code></strong> — the body stream can only be read once. Always read as text first.</p></li><li><p><strong>File IDs are server-assigned</strong> — when adding files to existing uploads, don&#39;t pass a <code>fileId</code> in the URL. The server creates one.</p></li><li><p><strong><code>uploadToken</code> must be in <code>X-UploadToken</code> header</strong> — not in the request body or URL query for API calls.</p></li><li><p><strong>During uploads, <code>activeFiles</code> filters by readiness</strong> — non-streaming: only <code>uploaded</code>; streaming: <code>uploading</code> + <code>uploaded</code>. When not uploading (friend viewing), all non-removed statuses are shown. Don&#39;t change this to a simple blacklist.</p></li><li><p><strong>Refreshing the page loses admin access</strong> — tokens are in-memory only. The only way to regain access is to open the Admin URL again.</p></li><li><p><strong>Delete responses are plain text <code>&quot;ok&quot;</code></strong> — don&#39;t try to parse <code>.message</code> from them. Always <code>fetchUpload()</code> after mutations.</p></li><li><p><strong>One-shot files disappear after download</strong> — their status changes server-side; re-fetching will show them as removed/missing.</p></li><li><p><strong>The Admin URL sidebar truncation uses <code>overflow-hidden</code> + <code>min-w-0</code></strong> — without this, long URLs push the entire mobile layout wider than the viewport.</p></li><li><p><strong><code>generateRef()</code> is for local tracking only</strong> — it creates monotonically increasing IDs that are never sent to the server.</p></li><li><p><strong>Vite dev server runs on port 5173/5174</strong> — the Go backend runs on port 8080. During dev, Vite proxies API calls to the backend via <code>vite.config.js</code>.</p></li><li><p><strong><code>webapp/dist/</code> is gitignored</strong> — never commit build artifacts. The CI/Docker build produces them fresh.</p></li><li><p><strong>DownloadView has two error refs</strong> — <code>error</code> (page-level) and <code>uploadError</code> (inline banner). Setting file upload errors on <code>error</code> hides the entire upload content due to template branching. Always use <code>uploadError</code> for file transfer failures.</p></li><li><p><strong>Filenames are capped at 1024 characters</strong> — enforced in <code>UploadView.addFiles()</code>, <code>FileRow.onNameInput/onNameKeydown/onNamePaste</code>. The server also validates this, so both layers must agree.</p></li><li><p><strong>E2EE passphrase is never stored server-side</strong> — it lives only in the <code>pendingUploadStore</code> (for same-session navigation) and optionally in the URL fragment (via the share toggle). If the user loses the passphrase, decryption is impossible.</p></li></ol><hr><h2 id="end-to-end-encryption-e2ee" tabindex="-1">End-to-End Encryption (E2EE) <a class="header-anchor" href="#end-to-end-encryption-e2ee" aria-label="Permalink to &quot;End-to-End Encryption (E2EE)&quot;">​</a></h2><h3 id="module-crypto-js" tabindex="-1">Module: <code>crypto.js</code> <a class="header-anchor" href="#module-crypto-js" aria-label="Permalink to &quot;Module: \`crypto.js\`&quot;">​</a></h3><p>Provides streaming encryption/decryption using the <code>age-encryption</code> npm package:</p><table tabindex="0"><thead><tr><th>Function</th><th>Description</th></tr></thead><tbody><tr><td><code>encryptFile(file, passphrase)</code></td><td>Encrypts a <code>File</code> object → returns encrypted <code>File</code></td></tr><tr><td><code>fetchAndDecrypt(url, passphrase)</code></td><td>Fetches encrypted bytes, decrypts → returns <code>Blob</code></td></tr><tr><td><code>generatePassphrase()</code></td><td>Generates a 20-char cryptographically-secure passphrase</td></tr></tbody></table><h3 id="upload-flow-e2ee" tabindex="-1">Upload Flow (E2EE) <a class="header-anchor" href="#upload-flow-e2ee" aria-label="Permalink to &quot;Upload Flow (E2EE)&quot;">​</a></h3><ol><li>User toggles E2EE in <code>UploadSidebar</code> → passphrase auto-generated (or customized)</li><li><code>UploadView.doUpload()</code> encrypts each file via <code>encryptFile()</code> before building the upload params</li><li><code>params.e2ee = &#39;age&#39;</code> sent to server → server stores the E2EE scheme on the upload model</li><li>Passphrase passed via <code>setPendingFiles(id, files, basicAuth, passphrase)</code> to the pending store</li><li>Navigation to DownloadView — passphrase is <strong>not</strong> in the URL</li></ol><h3 id="download-flow-e2ee" tabindex="-1">Download Flow (E2EE) <a class="header-anchor" href="#download-flow-e2ee" aria-label="Permalink to &quot;Download Flow (E2EE)&quot;">​</a></h3><ol><li><code>DownloadView.onMounted()</code> reads passphrase from <code>pendingUploadStore</code> (same-session) or URL fragment <code>#key=</code> (shared link)</li><li>If E2EE is set on the upload but no passphrase is available → modal prompt</li><li>Passphrase is stripped from the URL after extraction (security measure)</li><li><code>decryptAndDownload()</code> fetches the encrypted file and decrypts in-browser via <code>fetchAndDecrypt()</code></li><li>For E2EE files, <code>FileRow</code> emits <code>decrypt-download</code> instead of using a direct download link</li></ol><h3 id="server-behavior-for-e2ee-uploads" tabindex="-1">Server Behavior for E2EE Uploads <a class="header-anchor" href="#server-behavior-for-e2ee-uploads" aria-label="Permalink to &quot;Server Behavior for E2EE Uploads&quot;">​</a></h3><ul><li><strong>Browser redirect</strong>: <code>GetFile</code> handler checks <code>common.IsPlikWebapp(req)</code> (via <code>X-ClientApp: web_client</code> header) — if the request is from the webapp and the upload has <code>E2EE != &quot;&quot;</code>, it redirects to <code>/#/?id=&lt;uploadId&gt;</code> so the webapp handles passphrase input and decryption</li><li><strong>Content-Type</strong>: E2EE uploads are always served as <code>application/octet-stream</code> — content-type detection on encrypted bytes is meaningless</li><li><strong>CLI downloads</strong>: Non-webapp requests get raw encrypted bytes directly (for piping to <code>age --decrypt</code>)</li></ul><h3 id="downloadsidebar-e2ee" tabindex="-1">DownloadSidebar (E2EE) <a class="header-anchor" href="#downloadsidebar-e2ee" aria-label="Permalink to &quot;DownloadSidebar (E2EE)&quot;">​</a></h3><ul><li><strong>🔐 Encrypted badge</strong>: Shown in upload info when <code>upload.e2ee</code> is truthy</li><li><strong>Passphrase display</strong>: Shown in Share section with copy button (only when passphrase is available)</li><li><strong>Include passphrase in link toggle</strong>: Off by default — appends <code>#key=&lt;passphrase&gt;</code> to the share URL when enabled</li></ul>`,227)])])}const k=t(o,[["render",d]]);export{u as __pageData,k as default};
