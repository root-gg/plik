import{_ as t,o as d,c as o,a2 as a}from"./chunks/framework.DBSoX6Zw.js";const g=JSON.parse('{"title":"Architecture — Server (server/)","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/server.md","filePath":"architecture/server.md"}'),s={name:"architecture/server.md"};function r(i,e,c,n,l,p){return d(),o("div",null,[...e[0]||(e[0]=[a(`<h1 id="architecture-—-server-server" tabindex="-1">Architecture — Server (<code>server/</code>) <a class="header-anchor" href="#architecture-—-server-server" aria-label="Permalink to &quot;Architecture — Server (\`server/\`)&quot;">​</a></h1><blockquote><p>Internals of the Plik HTTP server. For system-wide overview, see the root <a href="./system.html">ARCHITECTURE.md</a>.</p></blockquote><hr><h2 id="package-structure" tabindex="-1">Package Structure <a class="header-anchor" href="#package-structure" aria-label="Permalink to &quot;Package Structure&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>server/</span></span>
<span class="line"><span>├── main.go         ← entry point (calls cmd.Execute())</span></span>
<span class="line"><span>├── plikd.cfg       ← default configuration file</span></span>
<span class="line"><span>├── cmd/            ← CLI commands (cobra)</span></span>
<span class="line"><span>├── common/         ← shared types, config, feature flags, utilities</span></span>
<span class="line"><span>├── context/        ← custom request context</span></span>
<span class="line"><span>├── data/           ← data backend interface + implementations</span></span>
<span class="line"><span>├── handlers/       ← HTTP handler functions</span></span>
<span class="line"><span>├── metadata/       ← metadata backend (GORM)</span></span>
<span class="line"><span>├── middleware/     ← middleware chain components</span></span>
<span class="line"><span>└── server/         ← HTTP server setup + router</span></span></code></pre></div><hr><h2 id="cmd-—-cli-commands-cobra" tabindex="-1"><code>cmd/</code> — CLI Commands (Cobra) <a class="header-anchor" href="#cmd-—-cli-commands-cobra" aria-label="Permalink to &quot;\`cmd/\` — CLI Commands (Cobra)&quot;">​</a></h2><p>The server binary <code>plikd</code> uses <a href="https://github.com/spf13/cobra" target="_blank" rel="noreferrer">cobra</a> for CLI management.</p><table tabindex="0"><thead><tr><th>File</th><th>Command</th><th>Description</th></tr></thead><tbody><tr><td><code>root.go</code></td><td><code>plikd</code></td><td>Start the server (default command)</td></tr><tr><td><code>user.go</code></td><td><code>plikd user create/list/delete</code></td><td>Manage local users</td></tr><tr><td><code>token.go</code></td><td><code>plikd token create/list/delete</code></td><td>Manage user tokens</td></tr><tr><td><code>file.go</code></td><td><code>plikd file list/delete</code></td><td>Manage uploads/files</td></tr><tr><td><code>clean.go</code></td><td><code>plikd clean</code></td><td>Run metadata cleanup</td></tr><tr><td><code>import.go</code></td><td><code>plikd import</code></td><td>Import metadata from gob + Snappy binary</td></tr><tr><td><code>export.go</code></td><td><code>plikd export</code></td><td>Export metadata to gob + Snappy binary</td></tr></tbody></table><p>Config loading order: <code>--config</code> flag → <code>PLIKD_CONFIG</code> env → <code>./plikd.cfg</code> → <code>/etc/plikd.cfg</code>.</p><hr><h2 id="common-—-shared-types-config" tabindex="-1"><code>common/</code> — Shared Types &amp; Config <a class="header-anchor" href="#common-—-shared-types-config" aria-label="Permalink to &quot;\`common/\` — Shared Types &amp; Config&quot;">​</a></h2><p>Core types used throughout the server:</p><table tabindex="0"><thead><tr><th>File</th><th>Content</th></tr></thead><tbody><tr><td><code>upload.go</code></td><td><code>Upload</code> struct — container for files with TTL, options, password</td></tr><tr><td><code>file.go</code></td><td><code>File</code> struct + status constants (<code>missing</code>/<code>uploading</code>/<code>uploaded</code>/<code>removed</code>/<code>deleted</code>)</td></tr><tr><td><code>user.go</code></td><td><code>User</code> struct + provider constants (<code>local</code>/<code>google</code>/<code>ovh</code>/<code>oidc</code>)</td></tr><tr><td><code>token.go</code></td><td><code>Token</code> struct — UUID-based upload tokens</td></tr><tr><td><code>cli_auth_session.go</code></td><td><code>CLIAuthSession</code> struct — ephemeral device auth sessions for CLI login</td></tr><tr><td><code>config.go</code></td><td><code>Configuration</code> struct — TOML parsing + env var override</td></tr><tr><td><code>feature_flags.go</code></td><td>Feature flag types: <code>disabled</code>/<code>enabled</code>/<code>default</code>/<code>forced</code></td></tr><tr><td><code>settings.go</code></td><td><code>Setting</code> struct — server-level key/value (e.g., auth signing key)</td></tr><tr><td><code>authentication.go</code></td><td><code>SessionAuthenticator</code> — JWT session cookie management</td></tr><tr><td><code>paging.go</code></td><td><code>PagingQuery</code> — pagination parameters</td></tr><tr><td><code>stats.go</code></td><td><code>ServerStats</code> — upload/file/user counts</td></tr><tr><td><code>metrics.go</code></td><td><code>PlikMetrics</code> — Prometheus metric registry</td></tr><tr><td><code>version.go</code></td><td>Build info (version, git commit, build date)</td></tr><tr><td><code>utils.go</code></td><td><code>GenerateRandomID()</code>, <code>StripPrefix()</code>, etc.</td></tr></tbody></table><hr><h2 id="context-—-custom-request-context" tabindex="-1"><code>context/</code> — Custom Request Context <a class="header-anchor" href="#context-—-custom-request-context" aria-label="Permalink to &quot;\`context/\` — Custom Request Context&quot;">​</a></h2><blockquote><p><strong>Historical note</strong>: This package predates Go&#39;s stdlib <code>context.Context</code> (added in Go 1.7). It provides a typed, mutex-protected struct that carries request-scoped values through the middleware chain.</p></blockquote><p>The <code>Context</code> struct holds:</p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Set By</th></tr></thead><tbody><tr><td><code>config</code></td><td><code>*Configuration</code></td><td>Server init</td></tr><tr><td><code>logger</code></td><td><code>*Logger</code></td><td>Server init</td></tr><tr><td><code>metadataBackend</code></td><td><code>*metadata.Backend</code></td><td>Server init</td></tr><tr><td><code>dataBackend</code></td><td><code>data.Backend</code></td><td>Server init</td></tr><tr><td><code>streamBackend</code></td><td><code>data.Backend</code></td><td>Server init</td></tr><tr><td><code>authenticator</code></td><td><code>*SessionAuthenticator</code></td><td>Server init</td></tr><tr><td><code>metrics</code></td><td><code>*PlikMetrics</code></td><td>Server init</td></tr><tr><td><code>sourceIP</code></td><td><code>net.IP</code></td><td><code>SourceIP</code> middleware</td></tr><tr><td><code>upload</code></td><td><code>*Upload</code></td><td><code>Upload</code> middleware</td></tr><tr><td><code>file</code></td><td><code>*File</code></td><td><code>File</code> middleware</td></tr><tr><td><code>user</code></td><td><code>*User</code></td><td><code>Authenticate</code> middleware</td></tr><tr><td><code>token</code></td><td><code>*Token</code></td><td><code>Authenticate</code> middleware</td></tr><tr><td><code>pagingQuery</code></td><td><code>*PagingQuery</code></td><td><code>Paginate</code> middleware</td></tr></tbody></table><p>All fields are accessed via getter/setter methods protected by a <code>sync.RWMutex</code>. Getters panic if a required field is nil (fail-fast pattern).</p><p>The <code>context</code> package also provides <code>Chain</code> — a composable middleware chain builder: <code>NewChain(mw...).Append(mw...).Then(handler)</code>.</p><hr><h2 id="data-—-data-backend" tabindex="-1"><code>data/</code> — Data Backend <a class="header-anchor" href="#data-—-data-backend" aria-label="Permalink to &quot;\`data/\` — Data Backend&quot;">​</a></h2><p>The <code>Backend</code> interface is minimal (3 methods):</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">type</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Backend</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    AddFile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">file</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">common</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">File</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">reader</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> io</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Reader</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">err</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    GetFile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">file</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">common</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">File</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">reader</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> io</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">ReadCloser</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">err</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    RemoveFile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">file</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">common</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">File</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">err</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="implementations" tabindex="-1">Implementations <a class="header-anchor" href="#implementations" aria-label="Permalink to &quot;Implementations&quot;">​</a></h3><table tabindex="0"><thead><tr><th>Package</th><th>Backend</th><th>Notes</th></tr></thead><tbody><tr><td><code>data/file</code></td><td>Local filesystem</td><td>Files stored in configurable directory</td></tr><tr><td><code>data/s3</code></td><td>Amazon S3 / MinIO</td><td>Supports SSE-C/S3 encryption and optional legacy Content-MD5 compatibility mode</td></tr><tr><td><code>data/swift</code></td><td>OpenStack Swift</td><td></td></tr><tr><td><code>data/gcs</code></td><td>Google Cloud Storage</td><td></td></tr><tr><td><code>data/stream</code></td><td>In-memory pipe</td><td>Blocks uploader until downloader connects — nothing stored</td></tr><tr><td><code>data/testing</code></td><td>In-memory map</td><td>For tests only</td></tr></tbody></table><hr><h2 id="metadata-—-metadata-backend-gorm" tabindex="-1"><code>metadata/</code> — Metadata Backend (GORM) <a class="header-anchor" href="#metadata-—-metadata-backend-gorm" aria-label="Permalink to &quot;\`metadata/\` — Metadata Backend (GORM)&quot;">​</a></h2><p>Uses GORM with gormigrate for schema management across SQLite3, PostgreSQL, and MySQL.</p><h3 id="key-behaviors" tabindex="-1">Key behaviors <a class="header-anchor" href="#key-behaviors" aria-label="Permalink to &quot;Key behaviors&quot;">​</a></h3><ul><li><strong>SQLite3</strong>: WAL mode + foreign keys enabled on connect</li><li><strong>Schema init</strong>: Auto-migrates <code>Upload</code>, <code>File</code>, <code>User</code>, <code>Token</code>, <code>Setting</code>, <code>CLIAuthSession</code> tables</li><li><strong>Migrations</strong>: Versioned via gormigrate — see <code>migrations.go</code></li><li><strong>Cleaning</strong>: <code>Clean()</code> removes orphan files, tokens, and expired CLI auth sessions (FK integrity)</li><li><strong>Metrics</strong>: GORM Prometheus plugin for DB stats</li></ul><h3 id="files" tabindex="-1">Files <a class="header-anchor" href="#files" aria-label="Permalink to &quot;Files&quot;">​</a></h3><table tabindex="0"><thead><tr><th>File</th><th>Purpose</th></tr></thead><tbody><tr><td><code>metadata.go</code></td><td>Backend init, config, shutdown, clean</td></tr><tr><td><code>migrations.go</code></td><td>Schema migration definitions</td></tr><tr><td><code>upload.go</code></td><td>Upload CRUD + listing + expiration</td></tr><tr><td><code>file.go</code></td><td>File CRUD + status updates</td></tr><tr><td><code>user.go</code></td><td>User CRUD + listing</td></tr><tr><td><code>token.go</code></td><td>Token CRUD + listing</td></tr><tr><td><code>cli_auth_session.go</code></td><td>CLI auth session CRUD (create, get by code, update, delete expired)</td></tr><tr><td><code>setting.go</code></td><td>Server settings key/value store</td></tr><tr><td><code>stats.go</code></td><td>Aggregate statistics queries</td></tr><tr><td><code>exporter.go</code></td><td>gob + Snappy export of all metadata</td></tr><tr><td><code>importer.go</code></td><td>gob + Snappy import</td></tr></tbody></table><h3 id="import-export" tabindex="-1">Import / Export <a class="header-anchor" href="#import-export" aria-label="Permalink to &quot;Import / Export&quot;">​</a></h3><p>The <code>plikd export</code> and <code>plikd import</code> commands dump and restore all metadata (users, tokens, uploads, files, settings) to/from a single binary file.</p><ul><li><strong>Format</strong>: Go <a href="https://pkg.go.dev/encoding/gob" target="_blank" rel="noreferrer">gob</a> encoding compressed with <a href="https://github.com/golang/snappy" target="_blank" rel="noreferrer">Snappy</a>. Architecture-independent (portable across <code>amd64</code>/<code>arm64</code>), streaming (constant memory), Go-specific (not human-readable).</li><li><strong>Export order</strong>: users → tokens → uploads (including soft-deleted) → files → settings. CLI auth sessions are intentionally excluded (ephemeral).</li><li><strong>Import</strong>: decodes sequentially, calls <code>Create*</code> on the metadata backend. Supports <code>--ignore-errors</code> to skip problematic records.</li><li><strong>Use cases</strong>: backend migration (e.g. SQLite → PostgreSQL), backups, disaster recovery.</li></ul><blockquote><p><strong>Note</strong>: Only metadata is exported — file data in the data backend must be migrated separately.</p></blockquote><h3 id="migration-dump-tests" tabindex="-1">Migration Dump Tests <a class="header-anchor" href="#migration-dump-tests" aria-label="Permalink to &quot;Migration Dump Tests&quot;">​</a></h3><p>When adding a new migration, <code>TestGenerateSQLDump</code> and <code>TestGenerateExport</code> in <code>migrations_test.go</code> auto-generate dump files on first run. Each dump captures the full DB state after all migrations and test data have been applied. <code>TestMigrationsFromSQLDumps</code> and <code>TestLoadExports</code> then load <strong>all</strong> existing dumps and verify migrations can be replayed forward to the current schema.</p><p><strong>Dump directory structure</strong>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>metadata/dumps/</span></span>
<span class="line"><span>├── sqlite3/      ← sqlite3 .dump output (generated by make test)</span></span>
<span class="line"><span>├── export/       ← gob + Snappy export   (generated by make test)</span></span>
<span class="line"><span>├── postgres/     ← pg_dump output        (generated by make test-backends)</span></span>
<span class="line"><span>├── mysql/        ← mysqldump output      (generated by make test-backends)</span></span>
<span class="line"><span>└── mariadb/      ← mariadb-dump output   (generated by make test-backends)</span></span></code></pre></div><p><strong>Two-stage generation process</strong>:</p><table tabindex="0"><thead><tr><th>Stage</th><th>Command</th><th>Backends</th><th>Infrastructure</th></tr></thead><tbody><tr><td>1</td><td><code>make test</code></td><td><code>sqlite3</code> + <code>export</code></td><td>Local, requires <code>sqlite3</code> CLI (<code>apt install sqlite3</code>)</td></tr><tr><td>2</td><td><code>make test-backends</code></td><td><code>postgres</code>, <code>mysql</code>, <code>mariadb</code></td><td>Docker containers (e.g., <code>plik.postgres</code>), dumps via <code>docker exec</code></td></tr></tbody></table><ol><li>Run <code>make test</code> locally → generates <code>sqlite3</code> + <code>export</code> dumps</li><li>Run <code>make test-backends</code> locally → generates <code>postgres</code>, <code>mysql</code>, <code>mariadb</code> dumps</li></ol><blockquote><p><strong>[!IMPORTANT] Always commit dump files</strong> in <code>server/metadata/dumps/</code> After adding a new migration</p></blockquote><p><strong>CI workflow</strong> (<code>.github/workflows/tests.yaml</code>): Both jobs run a <strong>&quot;Check for uncommitted changes&quot;</strong> step after tests — <code>git diff --exit-code</code> + <code>git ls-files --others</code> — which <strong>fails the build</strong> if any files were generated but not committed (e.g., missing dump files for a new migration)</p><p><strong>PostgreSQL 18+ compatibility</strong>: <code>pg_dump</code> now emits <code>\\restrict</code>/<code>\\unrestrict</code> psql meta-commands (CVE-2025-8714 sandbox). Since <code>loadSQLDump</code> executes dumps via <code>sqlDB.Exec()</code> (not <code>psql</code>), these are not valid SQL. The loader filters out all lines starting with <code>\\</code> before execution.</p><hr><h2 id="middleware-—-middleware-chain" tabindex="-1"><code>middleware/</code> — Middleware Chain <a class="header-anchor" href="#middleware-—-middleware-chain" aria-label="Permalink to &quot;\`middleware/\` — Middleware Chain&quot;">​</a></h2><p>Each middleware is a function that takes a <code>context.Context</code> and optionally calls the next handler.</p><table tabindex="0"><thead><tr><th>File</th><th>Middleware</th><th>Purpose</th></tr></thead><tbody><tr><td><code>context.go</code></td><td><code>Context()</code></td><td>Initialize context with server-level values</td></tr><tr><td><code>log.go</code></td><td><code>Log</code></td><td>Request/response logging</td></tr><tr><td><code>recover.go</code></td><td><code>Recover</code></td><td>Panic recovery → HTTP error response</td></tr><tr><td><code>source_ip.go</code></td><td><code>SourceIP</code></td><td>Extract client IP (supports <code>X-Forwarded-For</code> header)</td></tr><tr><td><code>authenticate.go</code></td><td><code>Authenticate(acceptToken)</code></td><td>Parse session cookie / X-PlikToken header → set user/token</td></tr><tr><td><code>impersonate.go</code></td><td><code>Impersonate</code></td><td>Admin impersonation support</td></tr><tr><td><code>upload.go</code></td><td><code>Upload</code></td><td>Resolve <code>{uploadID}</code> → load upload + check auth</td></tr><tr><td><code>file.go</code></td><td><code>File</code></td><td>Resolve <code>{fileID}</code> → load file from upload</td></tr><tr><td><code>create_upload.go</code></td><td><code>CreateUpload</code></td><td>Parse upload creation params for quick upload</td></tr><tr><td><code>paginate.go</code></td><td><code>Paginate</code></td><td>Parse pagination query params</td></tr><tr><td><code>redirect.go</code></td><td><code>RedirectOnFailure</code></td><td>Redirect to webapp on error (for browser requests)</td></tr><tr><td><code>block_bot_download.go</code></td><td><code>BlockBotDownload</code></td><td>Block messaging app link preview bots from downloading one-shot/streaming files (returns 406)</td></tr><tr><td><code>user.go</code></td><td><code>User</code></td><td>Resolve <code>{userID}</code> → load user (admin or self)</td></tr></tbody></table><hr><h2 id="handlers-—-http-handlers" tabindex="-1"><code>handlers/</code> — HTTP Handlers <a class="header-anchor" href="#handlers-—-http-handlers" aria-label="Permalink to &quot;\`handlers/\` — HTTP Handlers&quot;">​</a></h2><p>Each handler file contains one or more <code>http.Handler</code> functions.</p><table tabindex="0"><thead><tr><th>File</th><th>Handlers</th><th>Description</th></tr></thead><tbody><tr><td><code>create_upload.go</code></td><td><code>CreateUpload</code></td><td>Create upload with options, validate config/quotas</td></tr><tr><td><code>add_file.go</code></td><td><code>AddFile</code></td><td>Upload file to existing upload (multipart)</td></tr><tr><td><code>get_upload.go</code></td><td><code>GetUpload</code></td><td>Return upload metadata</td></tr><tr><td><code>get_file.go</code></td><td><code>GetFile</code></td><td>Download file, handle OneShot, extend TTL</td></tr><tr><td><code>get_archive.go</code></td><td><code>GetArchive</code></td><td>Download all files as zip</td></tr><tr><td><code>remove_file.go</code></td><td><code>RemoveFile</code></td><td>Mark file as removed</td></tr><tr><td><code>remove_upload.go</code></td><td><code>RemoveUpload</code></td><td>Soft-delete upload</td></tr><tr><td><code>misc.go</code></td><td><code>GetConfiguration</code>, <code>GetVersion</code>, <code>GetQrCode</code>, <code>Health</code></td><td>Utility endpoints</td></tr><tr><td><code>local.go</code></td><td><code>LocalLogin</code>, <code>Logout</code></td><td>Local auth</td></tr><tr><td><code>google.go</code></td><td><code>GoogleLogin</code>, <code>GoogleCallback</code></td><td>Google OAuth</td></tr><tr><td><code>ovh.go</code></td><td><code>OvhLogin</code>, <code>OvhCallback</code></td><td>OVH OAuth</td></tr><tr><td><code>oidc.go</code></td><td><code>OIDCLogin</code>, <code>OIDCCallback</code></td><td>OpenID Connect</td></tr><tr><td><code>cli_auth.go</code></td><td><code>CLIAuthInit</code>, <code>CLIAuthApprove</code>, <code>CLIAuthPoll</code></td><td>CLI device auth flow</td></tr><tr><td><code>me.go</code></td><td><code>UserInfo</code>, <code>DeleteAccount</code>, <code>GetUserStatistics</code></td><td>Current user</td></tr><tr><td><code>token.go</code></td><td><code>GetUserTokens</code>, <code>CreateToken</code>, <code>RevokeToken</code></td><td>Token management</td></tr><tr><td><code>user.go</code></td><td><code>GetUsers</code>, <code>CreateUser</code>, <code>UpdateUser</code></td><td>User management</td></tr><tr><td><code>admin.go</code></td><td><code>GetServerStatistics</code>, <code>GetUploads</code>, <code>RemoveUserUploads</code>, <code>GetUserUploads</code></td><td>Admin endpoints</td></tr></tbody></table><hr><h2 id="server-—-http-server-setup" tabindex="-1"><code>server/</code> — HTTP Server Setup <a class="header-anchor" href="#server-—-http-server-setup" aria-label="Permalink to &quot;\`server/\` — HTTP Server Setup&quot;">​</a></h2><p><code>PlikServer</code> is the main server struct. It:</p><ol><li>Initializes backends (metadata, data, stream) and authenticator</li><li>Builds middleware chains (see root ARCHITECTURE.md for chain table)</li><li>Configures gorilla/mux router with all routes</li><li>Starts HTTP server via <code>net.Listen</code> + <code>httpServer.Serve</code> (supports ephemeral port allocation with <code>ListenPort: 0</code>)</li><li>Starts cleaning routine (if auto-clean enabled)</li><li>Starts metrics HTTP server (if configured)</li></ol><p>After start, call <code>GetListenPort()</code> to retrieve the actual listen port (useful when configured with port 0).</p><p>Shutdown: graceful with configurable timeout, closes HTTP server + metadata backend.</p>`,62)])])}const u=t(s,[["render",r]]);export{g as __pageData,u as default};
