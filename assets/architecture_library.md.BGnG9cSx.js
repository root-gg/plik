import{_ as s,o as a,c as t,a2 as i}from"./chunks/framework.DBSoX6Zw.js";const k=JSON.parse('{"title":"Architecture — Go Library (plik/)","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/library.md","filePath":"architecture/library.md"}'),n={name:"architecture/library.md"};function l(o,e,d,r,p,h){return a(),t("div",null,[...e[0]||(e[0]=[i(`<h1 id="architecture-—-go-library-plik" tabindex="-1">Architecture — Go Library (<code>plik/</code>) <a class="header-anchor" href="#architecture-—-go-library-plik" aria-label="Permalink to &quot;Architecture — Go Library (\`plik/\`)&quot;">​</a></h1><blockquote><p>Public Go client library for the Plik API. For system-wide overview, see the root <a href="./system.html">ARCHITECTURE.md</a>.</p></blockquote><hr><h2 id="structure" tabindex="-1">Structure <a class="header-anchor" href="#structure" aria-label="Permalink to &quot;Structure&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plik/</span></span>
<span class="line"><span>├── README.md          ← library usage documentation</span></span>
<span class="line"><span>├── client.go          ← Client type — server connection and config</span></span>
<span class="line"><span>├── upload.go          ← Upload type — create/manage uploads</span></span>
<span class="line"><span>├── file.go            ← File type — upload/download files</span></span>
<span class="line"><span>├── internal.go        ← internal HTTP request helpers</span></span>
<span class="line"><span>├── setup_test.go      ← test server setup</span></span>
<span class="line"><span>├── *_test.go          ← unit tests</span></span>
<span class="line"><span>└── z*_e2e_*_test.go   ← end-to-end tests (ordered by z-prefix)</span></span></code></pre></div><hr><h2 id="public-api" tabindex="-1">Public API <a class="header-anchor" href="#public-api" aria-label="Permalink to &quot;Public API&quot;">​</a></h2><h3 id="client-client-go" tabindex="-1"><code>Client</code> (<code>client.go</code>) <a class="header-anchor" href="#client-client-go" aria-label="Permalink to &quot;\`Client\` (\`client.go\`)&quot;">​</a></h3><p>Factory for creating uploads. Holds server URL, login/password, and upload token.</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">client </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> plik.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">NewClient</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;http://localhost:8080&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><h3 id="upload-upload-go" tabindex="-1"><code>Upload</code> (<code>upload.go</code>) <a class="header-anchor" href="#upload-upload-go" aria-label="Permalink to &quot;\`Upload\` (\`upload.go\`)&quot;">​</a></h3><p>Represents an upload with options. Created via <code>client.NewUpload()</code>.</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">upload </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">NewUpload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">upload.OneShot </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">upload.TTL </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3600</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">upload.Token </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;xxxx-xxxx-xxxx&quot;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // Optional: CLI token to link upload to a user</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">file </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> upload.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">AddFileFromReader</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;myfile.txt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, reader)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">err </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> upload.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Upload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// POST /upload + upload files</span></span></code></pre></div><p><code>Login</code>/<code>Password</code> on <code>UploadParams</code> are for downloading <strong>password-protected</strong> uploads, not user authentication.</p><h3 id="file-file-go" tabindex="-1"><code>File</code> (<code>file.go</code>) <a class="header-anchor" href="#file-file-go" aria-label="Permalink to &quot;\`File\` (\`file.go\`)&quot;">​</a></h3><p>Represents a file within an upload. Supports upload, download, and removal.</p><hr><h2 id="e2e-test-suite" tabindex="-1">E2E Test Suite <a class="header-anchor" href="#e2e-test-suite" aria-label="Permalink to &quot;E2E Test Suite&quot;">​</a></h2><p>The e2e tests are the most comprehensive test layer in Plik. They spin up a <strong>real plikd server</strong> in-process and exercise the full HTTP API through the Go client library. The <code>z</code> prefix on filenames ensures Go runs them after unit tests (Go sorts test functions by file name within a package).</p><h3 id="test-infrastructure-setup-test-go" tabindex="-1">Test Infrastructure (<code>setup_test.go</code>) <a class="header-anchor" href="#test-infrastructure-setup-test-go" aria-label="Permalink to &quot;Test Infrastructure (\`setup_test.go\`)&quot;">​</a></h3><h4 id="testmain-—-global-bootstrap" tabindex="-1"><code>TestMain</code> — Global Bootstrap <a class="header-anchor" href="#testmain-—-global-bootstrap" aria-label="Permalink to &quot;\`TestMain\` — Global Bootstrap&quot;">​</a></h4><p><code>TestMain</code> runs once before all tests in the package. It sets up the <strong>data backend</strong> and <strong>metadata backend</strong> that all tests share:</p><ol><li><strong>Config loading</strong>: If <code>PLIKD_CONFIG</code> env var is set, loads a full <code>plikd.cfg</code> from that path (used by the Docker-based backend tests in <code>testing/</code>). Otherwise, creates a default config.</li><li><strong>Data backend selection</strong>: Based on <code>testConfig.DataBackend</code>: <ul><li><code>testing</code> (default) — in-memory backend, no external deps</li><li><code>file</code> — temp directory, cleaned up after all tests</li><li><code>s3</code> — real S3/MinIO, configured via <code>data_backend_config</code> env var</li><li><code>swift</code> — real OpenStack Swift</li></ul></li><li><strong>Metadata backend</strong>: Defaults to SQLite3 at <code>/tmp/plik.test.db</code> with <code>EraseFirst: true</code> (wipes DB on startup). Overridden by <code>PLIKD_CONFIG</code> for PostgreSQL/MySQL testing.</li></ol><blockquote><p>⚠️ <strong>Important</strong>: Backends are NOT automatically cleared between individual tests. Each test creates uploads that persist across the test run. Tests must be written to not depend on a clean state.</p></blockquote><h4 id="newplikserverandclient-—-per-test-server-setup" tabindex="-1"><code>newPlikServerAndClient</code> — Per-Test Server Setup <a class="header-anchor" href="#newplikserverandclient-—-per-test-server-setup" aria-label="Permalink to &quot;\`newPlikServerAndClient\` — Per-Test Server Setup&quot;">​</a></h4><p>Each test function calls <code>newPlikServerAndClient()</code> to get a fresh server + client pair:</p><div class="language-go vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">go</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">ps, pc </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> newPlikServerAndClient</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">defer</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> shutdown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ps)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">err </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> startWithClient</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ps, pc)</span></span></code></pre></div><p>This creates:</p><ul><li>A new <code>PlikServer</code> with a fresh config listening on <code>127.0.0.1</code> with ephemeral port allocation (port 0 → OS assigns a free port)</li><li>Injects the shared data + metadata backends</li><li>A new <code>Client</code> whose URL is set by <code>startWithClient</code> to the actual listen address after boot</li><li>Auto-clean is disabled (<code>config.AutoClean(false)</code>) so TTL-based cleanup doesn&#39;t interfere with tests</li></ul><p>The <code>start()</code> helper starts the server and waits until the HTTP port is accepting connections. <code>startWithClient()</code> additionally updates the client URL with the actual port. <code>shutdown()</code> calls <code>ShutdownNow()</code> (immediate, no graceful drain).</p><p>Each test gets its own server instance with its own router/middleware stack, but they all share the same underlying data and metadata backends. This means tests can configure the server differently (e.g., enable/disable features, set limits) without affecting other tests&#39; server config, while still sharing the same storage layer.</p><h4 id="test-utilities" tabindex="-1">Test Utilities <a class="header-anchor" href="#test-utilities" aria-label="Permalink to &quot;Test Utilities&quot;">​</a></h4><ul><li><strong><code>LockedReader</code></strong> — wraps an <code>io.Reader</code> with a channel-based lock. <code>Read()</code> blocks until <code>Unleash()</code> is called. Used to test concurrent upload/download scenarios and verify file status transitions (<code>missing</code> → <code>uploading</code> → <code>uploaded</code>).</li><li><strong><code>NewSlowReader</code> / <code>NewSlowReaderRandom</code></strong> — variant that auto-unleashes after a random delay (0–1s). Used in concurrency stress tests to simulate realistic upload timing.</li></ul><h3 id="test-files" tabindex="-1">Test Files <a class="header-anchor" href="#test-files" aria-label="Permalink to &quot;Test Files&quot;">​</a></h3><table tabindex="0"><thead><tr><th>File</th><th>Focus</th><th>Key Scenarios</th></tr></thead><tbody><tr><td><code>z1_e2e_test.go</code></td><td>Core upload/download flows</td><td>Upload twice (error), download during upload (status transitions), OneShot (delete after download), Removable, Stream mode, TTL expiration, Quick upload (curl-style multipart), forbidden options sanitization</td></tr><tr><td><code>z2_e2e_config_test.go</code></td><td>Server config enforcement</td><td>Path prefix, MaxFileSize, MaxFilePerUpload, AnonymousUpload disabled, DefaultTTL, TTL limits, Password/OneShot/Removable disabled, DownloadDomain + aliases, UploadWhitelist, SourceIpHeader</td></tr><tr><td><code>z3_e2e_authentication_test.go</code></td><td>Auth flows &amp; token isolation</td><td>CLI token auth, multi-token isolation (user can&#39;t control uploads from different token), admin token isolation (admin tokens don&#39;t grant extra power — by design, to limit impact of token leak), password-protected uploads</td></tr><tr><td><code>z4_e2e_client_concurrent_test.go</code></td><td>Concurrency &amp; race conditions</td><td>Multiple uploads in parallel, multiple files per upload in parallel, busy-loop upload (continuous <code>Upload()</code> calls with concurrent file adds), parallel download of same file, parallel create + get</td></tr><tr><td><code>z5_e2e_browser_auth_test.go</code></td><td>Browser-like authentication</td><td>Local login (cookie jar + XSRF flow), invalid password, login disabled, OIDC login via Keycloak (full redirect flow with form parsing), OIDC redirect URL validation. Uses <code>insecureCookieJar</code> to strip <code>Secure</code> flag for HTTP-only test env. Requires Keycloak for OIDC tests (skipped if unavailable).</td></tr><tr><td><code>z6_e2e_cli_login_test.go</code></td><td>CLI device auth flow</td><td>CLI login init, poll, and token generation via <code>/auth/cli/*</code> endpoints</td></tr></tbody></table><h3 id="running" tabindex="-1">Running <a class="header-anchor" href="#running" aria-label="Permalink to &quot;Running&quot;">​</a></h3><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Default (in-memory backends, fast)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> plik</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> &amp;&amp; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">go</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./...</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># With a specific data backend</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">data_backend</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">file</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> go</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./...</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># With full backend config (used by testing/ Docker scripts)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PLIKD_CONFIG</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">/path/to/test.cfg</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> go</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> test</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./...</span></span></code></pre></div><p>See also <a href="./testing.html">testing/ARCHITECTURE.md</a> for Docker-based backend integration tests that drive these e2e tests against real database and storage backends.</p>`,38)])])}const u=s(n,[["render",l]]);export{k as __pageData,u as default};
